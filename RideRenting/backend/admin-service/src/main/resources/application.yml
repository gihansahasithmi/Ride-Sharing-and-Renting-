server:
  port: 8084

spring:
  application:
    name: admin-service

services:
  auth-service:
    url: http://localhost:8081
  bike-service:
    url: http://localhost:8082
  rental-service:
    url: http://localhost:8083

management:
  endpoints:
    web:
      exposure:
        include: health,info
